# Detector
torch>=2.0
torchaudio>=2.0
numpy>=1.24
scipy>=1.10
librosa>=0.10
soundfile>=0.12

# Server
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.6

# Telephony
twilio>=8.0        # Twilio Media Streams + RequestValidator (Fix 3 signature validation)

# Utilities
python-dotenv>=1.0
pyyaml>=6.0
speechbrain>=1.0.0
pyttsx3>=2.90

# Pub/Sub (multi-worker production; optional for single-worker dev)
redis[asyncio]>=5.0

# Persistence / async I/O
aiosqlite>=0.20.0
aiofiles>=23.2.1

# Testing
pytest>=8.0
anyio[trio]>=4.0   # async pytest support for httpx / starlette TestClient
httpx>=0.27.0      # async test client for FastAPI
