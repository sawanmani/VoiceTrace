; =============================================
; VoiceTrace — Module Configuration
; =============================================
;
; Ensure AudioSocket modules are loaded.
; These are required for the AudioSocket() dialplan application.

[modules]
autoload=yes

; Explicitly load AudioSocket support
load = res_audiosocket.so
load = app_audiosocket.so
