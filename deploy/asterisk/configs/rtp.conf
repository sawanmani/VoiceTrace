; =============================================
; VoiceTrace — RTP Configuration
; =============================================
;
; Port range for RTP media streams.
; Keep this wide enough for concurrent calls.

[general]
rtpstart=10000
rtpend=20000
